# Database driver: "local" (better-sqlite3) or "turso" (libSQL)
DB_DRIVER=local

# Local SQLite (when DB_DRIVER=local)
LOCAL_DB_PATH=./cipherdrill.db

# Turso (when DB_DRIVER=turso) - get these from turso.tech
# TURSO_DATABASE_URL=libsql://your-db.turso.io
# TURSO_AUTH_TOKEN=
